# The repository includes .env.android for the current public endpoint.
# Copy this file to .env.android.local only when overriding it locally.
# Replace the endpoint with your HTTPS domain before releasing the app.
VITE_SYNC_API_ORIGIN=http://8.148.197.54
